databases:
  - name: postgres
    ipAllowList: []
    databaseName: vinc_ventures_development
    user: zacharia

services:
  - type: web
    name: vinc_ventures_development
    env: ruby
    buildCommand: "./render-build.sh"
    startCommand: "bundle exec rails s"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: postgres
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false